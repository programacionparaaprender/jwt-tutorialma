spring:
  datasource:
    url: jdbc:postgresql://localhost:5434/bd-jwt
    username: sa
    password: '123'
    driverClassName: org.postgresql.Driver
  jpa:
    show-sql: 'true'
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: create